# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem "jekyll", "~> 4.2"

# These themes are used when previewing/testing the site locally but may not be necessary for deploying to GitPages
gem "jekyll-theme-midnight"
gem "jekyll-theme-minimal"
gem "minima"

group :jekyll_plugins do
  gem 'jekyll-sitemap'
  gem 'jekyll-feed'
  gem 'jekyll-seo-tag'
end
